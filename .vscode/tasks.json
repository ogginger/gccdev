{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build - gtest (g++)",
            "command": "g++",
            "type": "shell",
            "args": [
                "-g",
                "${workspaceFolder}/src/main.cpp",
                "-o",
                "${workspaceFolder}/build/main"
            ],
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ]
}